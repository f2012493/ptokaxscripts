--<LockVarTypes>
Checks.Share={
    MinShare=0, -- Min share in gigabytes
    MaxShare=2, -- Following is in terabytes, good to keep out some brainless fakers 
    Level=2, -- The next option defines who GET share checked.
    BadShareTodo=2 -- Values: 1 to disconnect, 2 to redirect
  }

Checks.Clone={
  PMOps=1, -- PM ops if clone found? (0/1) Bot will PM operators if clones found with same share and disconnected.
  Level=3, -- The next option defines who GET clone checked.
}