--<LockVarTypes>
RegManagement={
	ShowReg=2, -- Announcement of new user registrations - 0 - off, 1 - main, 2 - opchat (if the opchat plugin is not running, falls back to main).
	AllowSelfReg=1, -- Self-registration allowed? 0/1
	ShowSelfReg=2, -- Announcement of new self-registrations - 0 - off, 1 - main, 2 - opchat (if the opchat plugin is not running, falls back to main)
	SelfRegAutoPrefix="nope", -- This prefix will be added to the registered nick. Set to "nope" to disable.
	Levels={
AddOp=5,
    AddSuperOp=5,
    AddReg=5,
    AddVip=5,
    DeleteOp=5,
    DeleteSuperOp=5,
    DeleteReg=5,
    DeleteVip=5,
    UpGradeToOp=5,
    UpGradeToSuperOp=5,
    UpGradeToVip=5,
    DownGradeToOp=5,
    DownGradeToReg=5,
    DownGradeToVip=5,
    GetPass=4, -- get the pass for a nick if regged
    ShowOps=1,
    ShowSuperOps=1,
    ShowVips=1,
    ShowRegs=1	
	},
} -- SETTINGS
