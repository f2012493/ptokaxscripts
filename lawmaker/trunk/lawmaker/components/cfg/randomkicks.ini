--<LockVarTypes>
RandomKicks={
  OnOff=1, -- This sets whether you want random kick messages or not *by default*. (0/1) Note that you can change this, but when you restart the scripts, this value will count.
  Levels={
    Add=4, -- add a random kick message
    Delete=4, -- delete some messages
    OnOff=5, -- enable/disable random kick messages
    Show=4, -- show random kick msgs
  }
}