Logging={
  Levels={
    CleanLogs=5, -- prune the logs
    SearchLog=4, -- search for usernames in logs.
  },
  MaxAge=3 -- in weeks, logs older than this will be cleaned with !cleanlogs - can be overriden with command parameter (ie: !cleanlogs <maxage>)
}